# Моя первая попытка, моя первая любовь.
#Калькулятор V1.5

# colorama Цвета для консоли - ctrl+c -> ctrl+v

from colorama import init
from colorama import Fore, Back, Style
init()
print ( Fore.GREEN )

print ("Привет, я калькулятор версии 1.5 ")
name=input("как тебя зовут?:")
print("Приветсвую и приятно пзнакомиться," + name + "!")
print("теперь приступим к работе!" + name + ", запомни, 'end' в качестве знака операции завершит работу калькулятора.")
while True:

# сложение, умножение и вычитание, ну еще и возведение в квадрат:
    print("Интструкция ко мне:\n1 >сложение +,\n2 >вычитание -,\n3 >умножение *,\n4 >деление /,\n5 >возведение в степень **,\n6 >деление без остатка //,\n7 >деление    остатком %" )
    action = input("Теперь введи требуемое действие (+,-,*,/,**,%,//):")
    if action == 'end':
        print("До новых встреч," + name + "!")
        break
    if action in ('+', '-', '*','/' ,'**','%','//'):
    
    while True:
        print("Для выбора другого знака введи: 000")
        x = float(input("Введи первое число:"))
        if x == 000:
            break
        y = float(input("Введи второе число:")) 
        
        if action == '+':
            print ("%.2f" % (x+y)) 
            print ("Это было просто! если хочешь завершить, пропиши - end")
        elif action == '-':
            print ("%.2f" % (x-y))
            print ("Это было легко! если хочешь завершить, пропиши - end")
        elif action == '*':
            print ("%.2f" % (x*y))
            print ("Это было не сложно! если хочешь завершить, пропиши - end")
        elif action == '**':
            print ("%.2f" % (x**y))
            print ("Фуух, ну это было не просто! если хочешь завершить, пропиши - end")
#Деление:            
        elif action == '/':
            if y != 0:
                print ("%.2f" % (x/y))
                print ("Это было простецки! если хочешь завершить, пропиши - end")
            else:
                print("Деление на ноль запрещено!\nвведите коррекные значения.")
        elif action == '%':  #<-----Дополни это место, тут есть незаконченная работа!!!!
            if y != 0:
                print ("%.2f" % (x%y))
                print ("Скукотища! если хочешь завершить, пропиши - end")
            else:
                print("Деление на ноль запрещено!\nвведите коррекные значения.")
        elif action == '//': #<-----Дополни это место, тут есть незаконченная работа!!!!
            if y != 0:
                print ("%.2f" % (x//y))
                print ("ПРИКОЛ! если хочешь завершить, пропиши - end")
            else:
                print("Деление на ноль запрещено!\nвведите коррекные значения.")
input()
